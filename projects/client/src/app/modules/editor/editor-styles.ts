/**
 * TODO: Refactor and integrate to app-settings
 */
export interface EditorStyles {
  /**
   * Default font-size, should default to '14px'
   */
  fontSize?: string;
  /**
   * The font to display.
   * Should default to roboto
   */
  font?: string;
}
